#include <stdio.h>

/** 
 * main - Entry main
 *
 * Return: always 0
*/

int main(void)
{
	int reverse;

	for (reverse = 'z'; reverse>= 'a' ; reverse--)
	{

	putchar(reverse);
	
	}
	putchar('\n');
	return (0);
}
